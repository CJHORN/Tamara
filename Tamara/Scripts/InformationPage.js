
function updateInfo() {
    var idInfo = document.getElementsById('idTxt');
    var enteredIdInfo = document.getElementById('text_Id');
    var btnReg = document.getElementById('Button1');
    if (btnReg.onclick) {
        idInfo === enteredIdInfo.innerText;
    }
}

